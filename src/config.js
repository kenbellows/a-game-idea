/* Config.js
 *
 * Configure the canvas, initialize certain global variables, etc.
 *
 * Kept separate from globals.js to conceptually separate the acts of
 * defining global variables and setting global configurations.
 */

canvas.width  = 900;
canvas.height = 900;

